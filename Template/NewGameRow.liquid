<tr>
	<th scope="row">
		{% if icon %}
			{% include 'IconLink', type: icon %}
		{% else %}
			{{name}}
		{% endif %}
	</th>

	{% if data.size > 0 %}
		{% for column in data %}
			{% if column == "-" %}
				<td style="text-align: center; opacity: 0.7">—</td>
			{% elsif column.size %}
				{% if column.size == 3 %}
					{% assign style = 'font-size: 90%' %}
				{% elsif column.size > 3 %}
					{% assign style = 'font-size: 80%' %}
				{% endif %}
				<td style="{{style}}">
					{% include 'ResistanceNumbers', numbers: column, commas: commas, strong: false %}
				</td>
			{% else %}
				<td>
					{% if commas %}{{column | number}}{% else %}{{column}}{% endif %}
				</td>
			{% endif %}
		{% endfor %}
	{% else %}
		<td colspan="8" style="text-align: center;">
			<strong>Immune</strong>
		</td>
	{% endif %}
</tr>